name: Generate multilingual README

on:
  push:
    branches:
      - main
    paths:
      - "data/toilets.json"
      - "README.template.md"
      - "README.ja.template.md"
      - "README.en.template.md"
      - "generate_readme.py"
      - ".github/workflows/generate-readme.yml"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Generate multilingual README
        run: python generate_readme.py

      - name: Commit updated README files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add README.md README.ja.md README.en.md

          if git diff --cached --quiet; then
            echo "README files have no changes"
          else
            git commit -m "chore: update multilingual README"
            git push
          fi